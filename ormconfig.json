{
  "type": "postgres",
  "host": "127.0.0.1",
  "port": 5432,
  "username": "purrweb",
  "password": "purrweb",
  "database": "pikabu",
  "migrations": ["migration/*.ts"],
  "entities": ["src/**/*.entity.ts"],
  "seeds": ["seeds/**/*.seed.ts"],
  "factories": ["factories/**/*.factory.ts"],
  "cli": {
    "migrationsDir": "migration"
  }
}
